'use strict'

const store = {
}

const currentPlayer = 'X'

const moves = 0

const over = false

const winner = ''

const gamesWon = 0

const gamesLost = 0

const gamesDraw = 0

module.exports = {
  store,
  currentPlayer,
  moves,
//  gamesPlayed,
  over,
  winner,
  gamesWon,
  gamesLost,
  gamesDraw
}
