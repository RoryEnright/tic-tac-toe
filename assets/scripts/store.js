'use strict'

const store = {
}

const currentPlayer = 'X'

const moves = 1

const gamesPlayed = 0

const over = false

const winner = ''

module.exports = {
  store,
  currentPlayer,
  moves,
  gamesPlayed,
  over,
  winner
}
